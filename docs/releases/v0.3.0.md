# v0.3.0

**Released:** May 13, 2026 · [Download](https://github.com/eltu/idx/releases/tag/v0.3.0)

---

## ✨ New features

### `idx skills install` — editor skill installer

New command to install idx skills into your AI-powered editor. Supports GitHub Copilot, Claude Code, and Cursor.

```bash
idx skills install claude
idx skills install copilot --verbose
idx skills install cursor
```

- Clones the skills repository into a temp directory, runs the install script, and cleans up automatically
- Silent by default — pass `--verbose` to stream git and installer output in real time
- Requires `git` in `$PATH`

### Styled version output

`idx version` now renders an ASCII art logo alongside version, tagline, build date, and current directory.

```
 ██╗██████╗ ██╗  ██╗   IDX v0.3.0
 ██║██╔══██╗╚██╗██╔╝   fast BM25 code search
 ██║██║  ██║ ╚███╔╝    2026-05-13  03:24 UTC
 ██║██║  ██║ ██╔██╗    ~/my-project
 ██║██████╔╝██╔╝ ██╗
 ╚═╝╚═════╝ ╚═╝  ╚═╝
```

### CLI command groups

Commands are now organized into named groups in the help output.

| Group | Commands |
|---|---|
| Index Setup | `init`, `destroy` |
| Index Sync | `sync`, `watch`, `daemon`, `status` |
| Search | `search`, `inspect` |
| About | `version` |
| Tools | `skills` |

### Init cancellation support

The init progress TUI now handles `Ctrl+C` cleanly — context propagation ensures no partial state is left behind.

---

## 🐛 Improvements

- Clearer error messages for unindexed directories — lists affected paths and suggests `idx sync`
- Refined search result text formatting and JSON output consistency
- Cleaner error propagation in CLI dispatch

---

## 📖 Documentation

- New `docs/features/skills.md` — full contract for `idx skills install`
- Updated `docs/features/version.md` — documents the new logo output
- Updated `docs/features/errors.md` — skills errors and updated command list

---

## 🏗️ Architecture

- ADR 0013 — skills install uses `git clone` over curl-pipe for auditability; explicit required editor argument
- New `SkillsInstaller` port with `OSSkillsInstaller` adapter
- `SkillsInstallService` fully unit-tested with gomock
